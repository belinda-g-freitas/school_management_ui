enum Access { r, w, x, unknown }

enum Gender { man, woman, other }

enum Civility { m, mrs, miss, other }

// TD, TP, cours, autres
enum CourseType { tutorial, pw, course, other }

// diploma
// enum Grade { bts, dut, btec, license, master, doctor, professor, other }
